// Edit this before deploying to Vercel: point at your deployed Railway/GCP backend.
// Left as localhost for local dev against `bash scripts/dev.sh`.
window.KURUKSHETRA_API_BASE = window.KURUKSHETRA_API_BASE || "http://127.0.0.1:8000";
