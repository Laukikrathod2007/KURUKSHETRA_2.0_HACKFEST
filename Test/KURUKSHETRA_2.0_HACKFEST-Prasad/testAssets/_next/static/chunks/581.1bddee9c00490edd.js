"use strict";(self.webpackChunk_N_E=self.webpackChunk_N_E||[]).push([[581],{5581:function(e,n,t){t.r(n),t.d(n,{default:function(){return u}});var r=t(5893),i=t(4250),o=t(7294),a=t(5319);let s=e=>{let n=(0,o.useRef)(null),t=(0,o.useRef)(null),r=(0,o.useRef)(null),i=(0,o.useRef)(null);return(0,o.useEffect)(()=>{if(!e)return;if(!r.current){let e=window.AudioContext||window.webkitAudioContext;r.current=new e}let o=r.current,a=()=>{"suspended"===o.state&&o.resume()};if(document.addEventListener("click",a),document.addEventListener("keydown",a),document.addEventListener("touchstart",a),!n.current){n.current=o.createAnalyser(),n.current.fftSize=512;let e=n.current.frequencyBinCount;t.current=new Uint8Array(e)}if(!i.current)try{i.current=o.createMediaElementSource(e),i.current.connect(n.current),n.current.connect(o.destination)}catch(e){console.error("Error connecting audio source:",e)}return()=>{document.removeEventListener("click",a),document.removeEventListener("keydown",a),document.removeEventListener("touchstart",a)}},[e]),{getFrequencyData:()=>{if(n.current&&t.current){var e;n.current.getByteFrequencyData(t.current);let i=(null===(e=r.current)||void 0===e?void 0:e.sampleRate)||44100,o=n.current.fftSize,a=n.current.frequencyBinCount,s=Math.max(1,Math.floor(250/(i/o))),u=0,l=0;for(let e=0;e<=s&&e<a;e++)u+=t.current[e],l++;let f=l>0?u/l:0,c=s+1,v=Math.max(c+1,Math.floor(4e3/(i/o))),d=0,m=0;for(let e=c;e<=v&&e<a;e++)d+=t.current[e],m++;let h=m>0?d/m:0,g=v+1,p=0,w=0;for(let e=g;e<a;e++)p+=t.current[e],w++;let x=w>0?p/w:0,y=0;for(let e=0;e<t.current.length;e++)y+=t.current[e];let B=y/t.current.length,M=[],E=[[0,1],[1,3],[3,6],[6,12],[12,24],[24,48],[48,96],[96,150],[150,200],[200,256]];for(let e=0;e<10;e++){let n=E[e],r=0,i=0,o=Math.min(n[0],a-1),s=Math.min(n[1],a);for(let e=o;e<s;e++)r+=t.current[e],i++;let u=1+.3*e,l=Math.pow((i>0?r/i:0)/255,3);M.push(Math.min(1,l*u))}return{data:t.current,average:B/255,low:f/255,mid:h/255,high:x/255,frequencyBands:M}}return{data:new Uint8Array(0),average:0,low:0,mid:0,high:0,frequencyBands:Array(10).fill(0)}}}};var u=e=>{let{audioRef:n}=e,t=(0,o.useRef)(null),u=(0,o.useRef)(null);(0,o.useRef)(null);let{viewport:l}=(0,a.D)(),{getFrequencyData:f}=s(n.current),c=(0,a.H)(i.dpR,"/images/messerangriff.png"),v=(0,o.useMemo)(()=>({uTexture:{value:c},uTime:{value:0},uResolution:{value:new i.FM8(l.width,l.height)},uImageResolution:{value:new i.FM8(1e3,1e3)},uFrequency:{value:0},uBass:{value:0},uMid:{value:0},uHigh:{value:0}}),[c,l.width,l.height]);return(0,o.useMemo)(()=>{let e=.8*l.width/10;return Array(10).fill(0).map((n,t)=>({position:[(t-4.5)*(e+.2),-l.height/3,0],key:t}))},[l.width,l.height]),(0,a.F)(e=>{let{clock:n}=e,t=f();if(t.frequencyBands&&u.current){u.current.uniforms.uTime.value=n.getElapsedTime();let e=(t.frequencyBands[2]+t.frequencyBands[3]+t.frequencyBands[4]+t.frequencyBands[5]+t.frequencyBands[6])/5,r=2*t.frequencyBands[1];u.current.uniforms.uBass.value=i.M8C.lerp(u.current.uniforms.uBass.value,e,.1),u.current.uniforms.uHigh.value=i.M8C.lerp(u.current.uniforms.uHigh.value,r,.1)}}),(0,r.jsx)(r.Fragment,{children:(0,r.jsxs)("mesh",{ref:t,position:[0,0,-1],children:[" ",(0,r.jsx)("planeGeometry",{args:[l.width,l.height,50,50]}),(0,r.jsx)("shaderMaterial",{ref:u,uniforms:v,vertexShader:'\nvarying vec2 vUv;\nuniform float uTime;\nuniform float uBass;\nuniform float uMid;\nuniform float uHigh;\nuniform float uFrequency; \n\n// Helper random function\nfloat random(vec2 st) {\n    return fract(sin(dot(st.xy, vec2(12.9898,78.233))) * 43758.5453123);\n}\n\nvoid main() {\n    vUv = uv;\n    vec3 pos = position;\n    \n    float gridX = 50.0;\n    float gridY = 50.0;\n    \n    vec2 gridIndex = floor(uv * vec2(gridX, gridY));\n    float r = random(gridIndex);\n    \n    // --- SMOOTH ANALOG RESPONSE ---\n    // Removed hard "if > valve" logic.\n    // Using smoothstep + pow to create a "soft knee" response.\n    // Quiet sounds (0.0 - 0.2) -> very tiny movement\n    // Loud sounds (0.5 - 1.0) -> exponential growth\n    \n    float rawSignal = uBass; // This is the mid-range average passed from scene\n    \n    // Smoothstep: clips negative values but smoothly ramps up from 0.15 to 1.0\n    float effectiveSignal = smoothstep(0.15, 1.0, rawSignal);\n    \n    // Exponential curve for impact\n    effectiveSignal = pow(effectiveSignal, 2.5);\n    \n    // Boost to visible range\n    effectiveSignal *= 3.0;\n\n    float displacement = 0.0;\n    \n    // Main chunks move with signal\n    if (r > 0.6) {\n        displacement += effectiveSignal; \n    }\n    \n    // Mids/Highs shimmer\n    displacement += uMid * 0.1;\n    if (r < 0.2) displacement += uHigh * 0.05;\n    \n    // --- Z-AXIS DISPLACEMENT ---\n    // Removed "floor" stepping logic.\n    // Movement is now continuous and smooth (analog).\n    \n    float maxZ = 2.0; \n    \n    // Apply displacement directly\n    pos.z += displacement * maxZ;\n    \n    gl_Position = projectionMatrix * modelViewMatrix * vec4(pos, 1.0);\n}\n',fragmentShader:"\nuniform sampler2D uTexture;\nuniform float uTime;\nuniform vec2 uResolution;\nuniform vec2 uImageResolution;\nuniform float uFrequency;\nuniform float uBass;\nuniform float uMid;\nuniform float uHigh;\nvarying vec2 vUv;\n\nvoid main() {\n    float screenAspect = uResolution.x / uResolution.y;\n    float imageAspect = uImageResolution.x / uImageResolution.y;\n\n    float scale = 0.9;\n    \n    // Bass Valve for Scale\n    // Ensure intro is calm by using a valve\n    float bassValve = 0.2; // Lowered to match vertex shader so it actually triggers\n    float effectiveBass = 0.0;\n    \n    if (uBass > bassValve) {\n        effectiveBass = (uBass - bassValve) / (1.0 - bassValve);\n        effectiveBass = pow(effectiveBass, 2.0); // Exponential punch\n    }\n    \n    scale += effectiveBass * 0.05; // Only expand when bass hits hard\n\n    vec2 newUv = vUv;\n    \n    if (screenAspect > imageAspect) {\n        float newWidth = (imageAspect / screenAspect) * scale;\n        float newHeight = 1.0 * scale;\n        float offsetX = (1.0 - newWidth) / 2.0;\n        float offsetY = (1.0 - newHeight) / 2.0;\n        newUv.x = (vUv.x - offsetX) / newWidth;\n        newUv.y = (vUv.y - offsetY) / newHeight;\n    } else {\n        float newWidth = 1.0 * scale;\n        float newHeight = (screenAspect / imageAspect) * scale;\n        float offsetX = (1.0 - newWidth) / 2.0;\n        float offsetY = (1.0 - newHeight) / 2.0;\n        newUv.x = (vUv.x - offsetX) / newWidth;\n        newUv.y = (vUv.y - offsetY) / newHeight;\n    }\n\n    if (newUv.x < 0.0 || newUv.x > 1.0 || newUv.y < 0.0 || newUv.y > 1.0) {\n         gl_FragColor = vec4(0.05, 0.05, 0.05, 1.0); \n    } else {\n         // Chromatic Aberration Logic\n         // Map to uHigh (High frequency bands 8-9)\n         // Use valve logic to keep intro clean if needed, or just let it shimmer\n         \n         float highValve = 0.2;\n         float effectiveHigh = 0.0;\n         \n         if (uHigh > highValve) {\n             effectiveHigh = (uHigh - highValve) / (1.0 - highValve);\n             effectiveHigh = pow(effectiveHigh, 2.0);\n         }\n         \n         // Reduced intensity by 5x (0.05 -> 0.01)\n         float aberration = effectiveHigh * 0.01; \n         \n         vec4 texColor;\n         \n         // Sample Red channel shifted LEFT\n         texColor.r = texture2D(uTexture, newUv + vec2(aberration, 0.0)).r;\n         \n         // Sample Green channel CENTERED\n         texColor.g = texture2D(uTexture, newUv).g;\n         \n         // Sample Blue channel shifted RIGHT\n         texColor.b = texture2D(uTexture, newUv - vec2(aberration, 0.0)).b;\n         \n         // Alpha remains same\n         texColor.a = texture2D(uTexture, newUv).a;\n         \n         // Color manipulations keyed ONLY to effectiveBass\n         // Lowered threshold from 0.3 to 0.2 to make it trigger more often (approx 15-20% boost in frequency)\n         if (effectiveBass > 0.2) {\n             texColor.rgb += vec3(effectiveBass * 0.2); \n             texColor.rgb = mix(texColor.rgb, vec3(1.0) - texColor.rgb, effectiveBass * 0.5); \n             \n             // WIREFRAME PULSE EFFECT\n             float gridSize = 50.0;\n             vec2 grid = fract(newUv * gridSize);\n             float lineThickness = 0.1;\n             \n             if (grid.x < lineThickness || grid.y < lineThickness) {\n                 // Mix in green wireframe based on intensity\n                 texColor.rgb = mix(texColor.rgb, vec3(0.0, 1.0, 0.0), effectiveBass * 0.8);\n             }\n         }\n         \n         gl_FragColor = texColor;\n    }\n}\n",wireframe:!1})]})})}}}]);